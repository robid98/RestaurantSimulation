/** @type {import('next').NextConfig} */
const nextConfig = {
    // transpilePackages: ['types', 'utilities', 'types-server', 'utilities-server', 'tv-charting-library']
}

module.exports = nextConfig
